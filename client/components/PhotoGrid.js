import React from "react";
import { Link } from "react-router";

const PhotoGrid = React.createClass({
  render() {
    return (
      <div>Im the photo grid</div>
    )
  }
})

export default PhotoGrid;
